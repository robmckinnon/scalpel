%h1 New scraper

= error_messages_for :scraper

- form_for(:scraper, :url => collection_url) do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Create"

= link_to 'Back', collection_url